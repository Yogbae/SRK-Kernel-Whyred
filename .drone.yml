kind: pipeline
name: SRK Project

clone:
  depth: 1

steps:
- name: Building
  image: fadlyas/kernel_dockerfile
  environment:
    bot_token:
      from_secret:
      from_secret:
  commands:
  - bash build.sh
